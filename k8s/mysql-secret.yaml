# Replace stringData values before production. Never commit real secrets.
apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: asientos
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: "change-root-password"
  MYSQL_USER: "asientos"
  MYSQL_PASSWORD: "change-app-password"
  MYSQL_DATABASE: "asientos"
